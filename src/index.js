import './style.css';
// import { inputcontainer, addToDo, userInput } from './function.js';

// inputcontainer.addEventListener('keypress', (e) => {
//   if (e.key === 'Enter') {
//     e.preventDefault();
//     addToDo(userInput.value);
//   }
// });

// listContainer.addEventListener('DOMContentLoaded', tasks);
